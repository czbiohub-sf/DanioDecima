# This file is used to configure your project.
# Read more about the various options under:
# https://setuptools.pypa.io/en/latest/userguide/declarative_config.html
# https://setuptools.pypa.io/en/latest/references/keywords.html

[metadata]
name = decima
description = DanioDecima: a zebrafish-specific fork of Decima for predicting single-cell gene expression from DNA sequence.
author = Avantika Lal (upstream Decima); DanioDecima authors (this fork)
author_email = lal.avantika@gene.com
license = Genentech Non-Commercial Software License Version 1.0
license_files = LICENSE.txt
url = https://github.com/czbiohub-sf/DanioDecima
project_urls =
    Upstream Decima = https://github.com/Genentech/decima
    Upstream decima-applications = https://github.com/Genentech/decima-applications
#    Source = https://github.com/pyscaffold/pyscaffold/
#    Changelog = https://pyscaffold.org/en/latest/changelog.html
#    Tracker = https://github.com/pyscaffold/pyscaffold/issues
#    Conda-Forge = https://anaconda.org/conda-forge/pyscaffold
#    Download = https://pypi.org/project/PyScaffold/#files
#    Twitter = https://twitter.com/PyScaffold

# Change if running only on Windows, Mac or Linux (comma-separated)
platforms = any

# Add here all kinds of additional classifiers as defined under
# https://pypi.org/classifiers/
classifiers =
    Development Status :: 4 - Beta
    Programming Language :: Python


[options]
zip_safe = False
packages = find_namespace:
include_package_data = True
package_dir =
    =src

# Require a min/specific Python version (comma-separated conditions)
python_requires = >=3.10

# Direct dependencies of the decima package, derived from imports in
# src/decima/ and scripts/. Version floors reflect the gReLu conda env
# that DanioDecima was developed in; see ../environment.yml for the full
# pinned environment used for the published results.
install_requires =
    anndata
    bioframe
    captum>=0.5
    einops
    genomepy
    grelu>=1.1
    h5py
    huggingface-hub
    numpy
    pandas
    plotnine
    pymemesuite
    pytorch-lightning>=2.0
    scipy
    scikit-learn
    torch>=2.0
    torchmetrics
    tqdm
    wandb


[options.packages.find]
where = src
exclude =
    tests

[options.extras_require]
# Add here additional requirements for extra features, to install with:
# `pip install decima[PDF]` like:
# PDF = ReportLab; RXP

# Add here test requirements (semicolon/line-separated)
testing =
    setuptools
    pytest
    pytest-cov

[options.entry_points]
# Add here console scripts like:
# console_scripts =
#     script_name = decima.module:function
# For example:
# console_scripts =
#     fibonacci = decima.skeleton:run
# And any other entry points, for example:
# pyscaffold.cli =
#     awesome = pyscaffoldext.awesome.extension:AwesomeExtension

[tool:pytest]
# Specify command line options as you would do when invoking pytest directly.
# e.g. --cov-report html (or xml) for html/xml output or --junitxml junit.xml
# in order to write a coverage file that can be read by Jenkins.
# CAUTION: --cov flags may prohibit setting breakpoints while debugging.
#          Comment those flags to avoid this pytest issue.
addopts =
    --cov decima --cov-report term-missing
    --verbose
norecursedirs =
    dist
    build
    .tox
testpaths = tests
# Use pytest markers to select/deselect specific tests
# markers =
#     slow: mark tests as slow (deselect with '-m "not slow"')
#     system: mark end-to-end system tests

[devpi:upload]
# Options for the devpi: PyPI server and packaging tool
# VCS export must be deactivated since we are using setuptools-scm
no_vcs = 1
formats = bdist_wheel

[flake8]
# Some sane defaults for the code style checker flake8
max_line_length = 88
extend_ignore = E203, W503
# ^  Black-compatible
#    E203 and W503 have edge cases handled by black
exclude =
    .tox
    build
    dist
    .eggs
    docs/conf.py

[pyscaffold]
# PyScaffold's parameters when the project was created.
# This will be used when updating. Do not change!
version = 4.5
package = decima
extensions =
    github_actions
    no_skeleton
    pre_commit
